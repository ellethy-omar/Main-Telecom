/*
    # Ignore the status codes, I was making them up in my previous projects, 
    # I haven't actually looked at the proper docuemntation
    # these are resuable components, instead of asking AI for everytime to generate for the same schema,
    
    ? COMMON RESPONSES
    ? 400   $ref: '#/components/responses/InvalidParameters'
    ? 401   $ref: '#/components/responses/UnauthorizedError'
    ? 404   $ref: '#/components/responses/PropertyNotFound'
    ? 500   $ref: '#/components/responses/ServerError'
    ? 503   $ref: '#/components/responses/ServiceUnavailable'

    ! COMMON SCHEMAS
    ! $ref: '#/components/schemas/Agent'
    ! $ref: '#/components/schemas/Contact'
    ! $ref: '#/components/schemas/Ticket'
    ! $ref: '#/components/schemas/Call'
*/
